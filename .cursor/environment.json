{
  "name": "SentinelWP",
  "install": "sudo apt-get update && sudo apt-get install -y --no-install-recommends python3-venv && python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt -r requirements-dev.txt",
  "terminals": [
    {
      "name": "SentinelWP Server",
      "command": "source .venv/bin/activate && python app.py"
    }
  ],
  "ports": [
    {
      "name": "SentinelWP Web UI",
      "port": 8080
    }
  ]
}
